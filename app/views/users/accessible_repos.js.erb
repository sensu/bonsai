<% if !Rails.env.development? && @repo_names.empty? %>
  retryGetExtension();
<% else %> 
  $("#extensions-selector").html(
   "<%= raw escape_javascript( render 'users/extension_selector' ) %>"
  );
<% end %>