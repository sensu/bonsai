<div class="heading-with-buttons">
  <h2 class="title"><%= title %></h2>

  <% if current_user == user %>
    <div class="buttons">
      <%= link_to "Share #{t('indefinite_articles.extension')} #{t('nouns.extension').titleize}", new_extension_path, class: "button radius tiny primary", rel: "new_extension" %>
    </div>
  <% end %>
</div>
